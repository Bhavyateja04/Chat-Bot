# --- Discord ---
# 2.5.2+ is required on Python 3.13: earlier versions import the `audioop`
# module, which was removed from the standard library in 3.13.
discord.py==2.5.2
# Restores the stdlib `audioop` module that discord.py's voice support imports.
audioop-lts==0.2.2; python_version >= "3.13"

# --- Document parsing ---
PyMuPDF==1.24.11
python-docx==1.1.2

# --- Validation / config ---
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- LLM (provider-agnostic, OpenAI-compatible HTTP) ---
httpx==0.27.2

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0

# --- Test fixture generation (PDF/DOCX creation) ---
reportlab==4.2.5
